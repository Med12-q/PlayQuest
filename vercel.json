{
  "buildCommand": "cd artifacts/playquest && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "artifacts/playquest/dist/public",
  "installCommand": "echo ok",
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
